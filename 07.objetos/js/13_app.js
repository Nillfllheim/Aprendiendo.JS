const producto = {
    nombre: 'Monitor de 19"',
    precio: 300,
    disponible: true,
}

console.log(Object.keys(producto)) //'keys' retorna en arreglo

console.log(Object.values(producto)) //'values' retorna los valores

console.log(Object.entries(producto)) // 'entries' retorna todo el objeto