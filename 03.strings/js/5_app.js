const producto = 'Monitor 24"'

//Reemplazando con (.replace())
console.log(producto.replace('Monitor', 'Televisor'))

//Cortando la primera letra tipo Gmail
const nombre = 'Nilson'

console.log(nombre.charAt(0))